# Тест по NAT и межсетевым экранам

1. Какие утверждения про NAT верные? (1 из 1 балла)
   * ✅ **Устройство NAT хранит записи соответствия внутренних и внешних IP адресов** (Правильно!)
   * ❌ **При работе динамического NAT в глобальную сеть имеют возможность обращаться все устройства в локальной сети одновременно**
   * ✅ **Технология NAT позволяет многим устройствам использовать один маршрутизируемый IP-адрес** (Правильно!)
   * ❌ **Динамический NAT осуществляет трансляцию IP-адресов с использованием номеров TCP-портов**
   * ✅ **Для трансляции нескольких внутренних адресов в один внешний используются порты транспортного уровня** (Правильно!)


2. На устройствах в локальной сети 10.0.0.0/24 на TCP-порту 31337 запущена программа, которая отправляет данные к глобальную сеть. Как в этом случае будет работать NAT типа «один ко многим»? (1 из 1 балла)
   * 🔴 **Устройство NAT преобразует локальные IP-адреса в глобальный IP-адрес, а все устройства в локальной сети будут получать данные, которые приходят на глобальный IP-адрес**
   * 🔴 **В этом случае NAT не используется**
   * 🟢 **Устройство NAT преобразует локальные IP-адреса в глобальные и поставит в соответствие один или несколько новых номеров портов** (Правильно!)
   * 🔴 **NAT будет использовать разные глобальные адреса**
   * 🔴 **Второе и следующие обратившиеся в сеть устройства будут ждать, так как NAT не сможет уникально отобразить несколько соединений соединения**


На межсетевом экране настроены следующие правила:
```
1. Разрешить передачу данных из сети 212.94.12.0/24 в любую сеть;
2. Отбрасывать все пакеты;
3. Разрешить передачу данных от любого IP-адреса с номером порта 80 в сеть 212.94.12.0/24.
```
Приходит пакет с IP-адресом назначения **212.94.12.230** и номером порта **80**.

3. Как межсетевой экран обработает данный пакет? (1 из 1 балла)
   * 🔴 **Межсетевой экран отбросит пакет по второму правилу, т.к. оно записано раньше правила для работы с портами**
   * 🟢 **Межсетевой экран разрешит передачу пакета по первому правилу** (Правильно!)
   * 🔴 **Межсетевой экран разрешит передачу пакета по третьему правилу**
   * 🔴 **Межсетевой экран отбросит пакет, так как в правилах фильтрации не прописаны порты для внутренней сети**


4. Какие утверждения про межсетевые экраны верные? (1 из 1 балла)
   * ✅ **Межсетевой экран может быть установлен на хосте** (Правильно!)
   * ❌ **Фильтрация трафика происходит только на сетевом уровне**
   * ❌ **Межсетевые экраны не влияют на связность компьютеров в сети между собой**
   * ✅ **Межсетевые экраны защищают внутреннюю сеть от несанкционированного доступа** (Правильно!)
   * ✅ **Межсетевые экраны используют IP адрес и номер порта для фильтрации трафика** (Правильно!)
