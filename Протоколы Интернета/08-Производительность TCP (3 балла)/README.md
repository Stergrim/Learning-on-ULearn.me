# Тест по производительности TCP

1. Выберите верные утверждения: (1 из 1 балла)
   * ❌ **Задержка сегмента является наиболее используемым сигналом о перегрузке**
   * ❌ **Окно управления потоком используется отправителем, чтобы задать количество сегментов, которое можно передать в сеть**
   * ❌ **Если получатель готов принимать новые данные, он устанавливает размер окна, равный 0**
   * ✅ **Специальные флаги в заголовках сетевого и транспортного уровней указывают на перегрузку в сети** (Правильно!)
   * ✅ **Размер окна используется для управления потоком** (Правильно!)


2. Выберите верные утверждения: (1 из 1 балла)
   * ❌ **Для определения размера окна TCP предпочитают использовать метод аддитивного увеличения мультипликативного уменьшения**
   * ✅ **Перегрузка в сети возникает, если передаётся большое количество пакетов, которое не успевают обрабатывать сетевые устройства** (Правильно!)
   * ✅ **Метод медленного старта увеличивает размер окна TCP экспоненциально** (Правильно!)
   * ✅ **В методе медленного старта на каждый подтверждённый сегмент отправляется два новых сегмента** (Правильно!)
   * ❌ **Чтобы отправитель узнал о проблемах в сети, должна произойти потеря переданного сегмента**


3. Какой механизм в TCP предотвращает отправку большого количества сегментов? (1 из 1 балла)
    * 🟢 `Окно перегрузки` (Правильный ответ: Окно перегрузки)
