# Тест по протоколам компьютерных сетей

1. Какие утверждения об ARP являются верными? (1 из 1 балла)
   * ✅ **Каждый хост хранит свою таблицу ARP** (Правильно!)
   * ❌ **ARP ответ передаётся широковещательно**
   * ✅ **ARP запрос получают все хосты в локальной сети** (Правильно!)
   * ✅ **ARP позволяет определить MAC-адрес хоста с указанным IP-адресом** (Правильно!)
   * ❌ **ARP позволяет по уже известному MAC-адресу определить IP-адрес**


2. Составьте процесс получения IP-адреса с помощью DHCP (1 из 1 балла)

| ✅            |
|--------------|
| **Discover** |
| **Offer**    |
| **Request**  |
| **Ack**      |


3. Какие утверждения о DHCP являются верными? (1 из 1 балла)
   * ✅ **Сообщения DHCP передаются по локальной сети** (Правильно!)
   * ❌ **Все сообщения DHCP передаются широковещательно**
   * ❌ **После истечения времени аренды хосту необходимо начать процесс получения IP-адреса заново**
   * ✅ **Хост получает IP-адрес на время аренды** (Правильно!)
   * ✅ **DHCP-сервер может выдавать различные настройки сети, такие как маска подсети или адрес шлюза сети** (Правильно!)


4. Какие утверждения про назначение IPv6-адресов верные? (1 из 1 балла)
   * ✅ **Процесс EUI-64 позволяет задать идентификатор интерфейса в IPv6-адресе** (Правильно!)
   * ❌ **В процессе SLAAC в качестве идентификатора интерфейса используется MAC-адрес**
   * ❌ **При работе SLAAC ведётся учёт назначенных IPv6-адресов**
   * ✅ **Идентификатор интерфейса в IPv6-адресе может назначаться случайно** (Правильно!)
   * ❌ **Все устройства в сети получают IPv6-адреса с помощью DHCPv6**


5. Какие утверждения про NDP верные? (1 из 1 балла)
   * ✅ **Протокол NDP позволяет узнать MAC-адрес по известному IPv6-адресу** (Правильно!)
   * ✅ **Сообщение Neighbor Solicitation является аналогом сообщения ARP Request (запрос)** (Правильно!)
   * ❌ **Сообщение Neighbor Advertisement является ответом на сообщение Router Solicitation**
   * ❌ **Сообщение Router Advertisement рассылается на адрес FF02::2**
   * ✅ **В сообщении Router Advertisement передаётся префикс IPv6-сети** (Правильно!)
   * ❌ **Протокол NDP заменяет протокол ICMP в IPv6 сетях**
