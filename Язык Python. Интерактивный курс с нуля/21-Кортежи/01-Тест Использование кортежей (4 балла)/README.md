# Тест: Использование кортежей

1. Какие из представленных вариантов создадут кортеж? (1 из 1 балла)
   * ✅ **t = tuple([1])** (Правильно!)
   * ❌ **t = tuple(1, 2)** (Будет ошибка, так как функция tuple ожидает один аргумент)
   * ❌ **t = tuple(1)** (Программа выведет ошибку, так как 1 не перечисляемый тип данных)
   * ✅ **t = (1, )** (Правильно!)
   * ❌ **t = (1)** (Для кортежей из одного элемента нужно использовать специальный синтаксис)
   * ✅ **t = (1, 2)** (Правильно!)
   * ❌ **t = ([1])** (Нужно добавить запятую)
   * ✅ **t = ()** (Правильно!)


```py
ts = ('Hello', )
print(len(ts))
```

2. Что выведет программа? (1 из 1 балла)
    * 🟢 `1` (Правильный ответ: 1)


```py
ts = tuple('Hello')
print(len(ts))
```

3. Что выведет программа? (1 из 1 балла)
    * 🟢 `5` (Правильный ответ: 5)


4. Выберите верные утверждения (1 из 1 балла)
   * ✅ **(1,) <= (1,)** (Правильно!)
   * ❌ **('a', 'b') > ('c', 'c', 'c')** (Кортежи сравниваются поэлементно, в первом элементе второй кортеж больше первого.)
   * ✅ **('a', 1) > ('a', 0)** (Правильно!)
   * ❌ **('a', 'b') > ('a', 1)** (Кортежи несравниваемые. При попытке сравнить такие пары возникнет ошибка.)
   * ❌ **('a', 'b') > ('c',)** (Кортежи сравниваются поэлементно, в первом элементе второй кортеж больше первого.)
   * ❌ **(1,) < (1,)**
   * ✅ **(1,) == (1,)** (Правильно!)
