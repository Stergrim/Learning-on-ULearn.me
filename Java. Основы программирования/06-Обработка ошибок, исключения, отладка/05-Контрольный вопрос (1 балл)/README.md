# Контрольный вопрос

Почему рекомендуется выводить отладочную информацию при помощи класса Logger, а не через System.out или System.err?

1. Тест — Выберите один или несколько вариантов из списка (1 из 1 балла)
   * ✅ **Logger поддерживает локализацию отладочных сообщений.** (Правильно!)
   * ✅ **Logger позволяет менять детальность логгирования, не прибегая к if'ам в коде.** (Правильно!)
   * ✅ **Logger дает возможность гибко настраивать формат и место записи сообщений без изменения программы.** (Правильно!)
   * ❌ **Logger сообщает об ошибках с уровнем WARNING и выше виртуальной машине, чтобы она могла их обработать.**
   * ❌ **Logger выводит текстовые сообщения более эффективно, чем System.out и System.err.**
